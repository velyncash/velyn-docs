- name: Basic docs check
  run: |
    (test -f README.md || test -f readme.md || test -f README.MD) || (echo "README file missing" && exit 1)
    test -d docs
    test -d specs
    echo "Docs structure OK ✅"
